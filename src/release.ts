/**
 * Immutable harness revision used by every generated reusable-workflow caller.
 *
 * This commit contains the complete, hosted-verified v0.4.1 workflow set,
 * including configurable requirement-to-spec output directories. Keeping one
 * full SHA here prevents individual templates from drifting to mutable branches,
 * future tags, or older incomplete releases.
 */
export const HARNESS_VERSION = "0.4.1";
export const DEFAULT_HARNESS_WORKFLOW_REF = "60353584d46b00f72c617d8f76eefaa4b333eaac";
