# Managed by pi-spec-harness: spec-to-issue-reference v1
name: Harness Spec to Issue

on:
  push:
    branches: [main]
    paths:
      - 'docs/specifications/*.md'

permissions:
  contents: read
  issues: write

jobs:
  spec-to-issue:
    uses: munichdeveloper/pi-spec-harness/.github/workflows/spec-to-issue.yml@655570967e5b323c51af77fb6c686a8768a91df8
    with:
      spec-path-glob: 'docs/specifications/*.md'
      default-branch: 'main'
      harness-ref: '655570967e5b323c51af77fb6c686a8768a91df8'
