# Managed by pi-spec-harness: spec-to-issue-reference v1
name: Harness Spec to Issue

on:
  push:
    branches: [main]
    paths:
      - 'docs/specifications/*.md'

permissions:
  contents: read
  issues: write

jobs:
  spec-to-issue:
    uses: munichdeveloper/pi-spec-harness/.github/workflows/spec-to-issue.yml@0dcce1129af2fd2f3a76259f87463c8714228062
    with:
      spec-path-glob: 'docs/specifications/*.md'
      default-branch: 'main'
      harness-ref: '0dcce1129af2fd2f3a76259f87463c8714228062'
