# Managed by pi-spec-harness: spec-to-issue-reference v1
name: Harness Spec to Issue

on:
  push:
    branches: [main]
    paths:
      - 'docs/specifications/*.md'

permissions:
  contents: read
  issues: write

jobs:
  spec-to-issue:
    uses: munichdeveloper/pi-spec-harness/.github/workflows/spec-to-issue.yml@8e74a892381afb92e90c80e60eb8b381bedfa0b8
    with:
      spec-path-glob: 'docs/specifications/*.md'
      default-branch: 'main'
      harness-ref: '8e74a892381afb92e90c80e60eb8b381bedfa0b8'
